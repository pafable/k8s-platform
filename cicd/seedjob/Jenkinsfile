final String platform_create = 'k8s-platform-create'
final String platform_repo = 'https://github.com/pafable/k8s-platform.git'
final String platform_branch = 'refs/head/master'
final String platform_script_path = 'cicd/deploy/Jenkinsfile'

pipeline {
    agent {
      label 'jenkins-jenkins-agent'
    }

    options {
        timeout(
            time: 5,
            unit: 'MINUTES'
        )
    }

    tools {
      terraform 'tf-1-9-2'
    }

    stages{
        stage('seeding') {
            steps {
                jobDsl scriptText: """
                    pipelineJob("${platform_create}") {
                        definition {
                            cpsScm {
                                scm {
                                    git {
                                        remote {
                                            url("${platform_repo}")
                                        }
                                        branch("${platform_branch}")
                                    }
                                }
                                scriptPath("${platform_script_path}")
                                lightweight()
                            }
                        }
                    }
                """
            }
        }
    }
}